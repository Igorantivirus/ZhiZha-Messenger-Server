#pragma once

#include <Protocol/Data/Rooms.hpp>

namespace utils
{
using UserId = protocol::UserId;
using RoomId = protocol::RoomId;
using MessageId = protocol::MessageId;
using Country = protocol::Country;

using RoomKind = protocol::RoomKind;
using JoinPolicy = protocol::JoinPolicy;
using WritePolicy = protocol::WritePolicy;
using MemberRole = protocol::MemberRole;

using RoomInfo = protocol::data::RoomInfo;

} // namespace utils