#pragma once

#include <string>

namespace utils
{

// Определяет IP машины в локальной сети кроссплатформенно (Asio).
// Приём: UDP-сокет "подключается" к публичному адресу — пакеты при этом
// не отправляются, но ОС выбирает исходящий интерфейс, и getsockname
// возвращает его адрес. Работает на Windows/Linux/macOS одинаково.
// При любой ошибке (нет сети и т.п.) возвращает "127.0.0.1".
std::string detectLocalAddress();

} // namespace utils
